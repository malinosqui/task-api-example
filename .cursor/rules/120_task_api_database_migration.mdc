---
description: Task API database migration readiness and data layer patterns
globs:
  - "src/**/*.{ts,tsx}"
alwaysApply: true
---

- **Migration-ready interfaces** – design DataStore interface to easily accommodate SQL/NoSQL databases.
- **Transaction patterns** – structure operations to support database transactions when migrating.
- **Query optimization preparation** – write data access patterns that translate well to database queries.
- **Connection pooling readiness** – design data layer to work with connection pools.
- **Schema evolution** – design models to support backwards-compatible schema changes.
- **Index considerations** – structure queries to utilize database indexes effectively.
- **Data consistency patterns** – implement patterns that ensure data consistency across operations.
