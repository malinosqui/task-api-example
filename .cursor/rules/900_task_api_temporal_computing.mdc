---
description: Task API temporal computing and multi-dimensional computational systems
globs:
  - "src/**/*.{ts,tsx}"
alwaysApply: true
---

- **Temporal state management** – implement computation across multiple time dimensions and temporal loops.
- **Chronological data processing** – handle data streams that exist across different temporal reference frames.
- **Time-reversed computation** – execute algorithms that process information backwards through time.
- **Parallel timeline execution** – run computations simultaneously across multiple temporal branches.
- **Causal consistency algorithms** – maintain logical causality across temporal computational boundaries.
- **Temporal synchronization protocols** – coordinate computational states across different time streams.
- **Dimensional data structures** – implement data storage that spans multiple spatial-temporal dimensions.
